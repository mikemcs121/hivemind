# Dependencies (reinstalled per machine via `npm install`)
node_modules/

# Build output (electron-builder)
dist/
out/

# Downloaded Whisper models (reproduced via `npm run fetch-model`, bundled at build time)
models/

# Logs
*.log
npm-debug.log*

# Claude Code per-machine local settings (not shared)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
